{
  "kind": "build_request",
  "title": "Restore original white and beige/brown color palette",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Restore the UI color palette to the original light theme using a white background and beige/brown (light brown) accents across the app.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-6"
        ],
        "quotes": [
          "torna ai colori originali bianco e marroncino"
        ]
      },
      "acceptanceCriteria": [
        "The app background renders as white (or near-white) across all pages (trainer, client, admin) without any dark/black theme appearance.",
        "Primary/accent UI elements (e.g., buttons, icons, gradient badges) use a beige/brown palette rather than blue/purple or dark-mode colors.",
        "Theme tokens in `frontend/src/index.css` are updated so Tailwind/shadcn components consistently reflect the white + beige/brown palette (background, primary, secondary, muted, accent, border, ring)."
      ]
    },
    {
      "id": "REQ-2",
      "text": "Remove or neutralize any remaining dark-theme styling so that the app cannot visually switch to a dark/black palette (including when the OS is in dark mode).",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-6"
        ],
        "quotes": [
          "torna ai colori originali bianco e marroncino"
        ]
      },
      "acceptanceCriteria": [
        "With OS-level dark mode enabled, the app still renders with the same white + beige/brown palette.",
        "There are no pages/components that render with dark background/foreground tokens due to a `.dark` class being applied or dark variables being active.",
        "No changes are required to any backend canisters or API behavior to achieve this."
      ]
    }
  ],
  "constraints": [
    "Do not edit any files under `frontend/src/components/ui` (read-only shadcn components).",
    "Do not edit immutable hook entrypoints listed in SYSTEM_CONTEXT (compose around them instead)."
  ],
  "nonGoals": [
    "Adding new product features unrelated to theming.",
    "Changing backend data models, authorization, or persistence."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  },
  "userProfileUpdate": {
    "goalsToAdd": [],
    "goalsToRemove": [],
    "preferencesToAdd": [],
    "preferencesToRemove": [],
    "miscToAdd": [],
    "miscToRemove": [],
    "fieldsToSet": {},
    "fieldsToDelete": []
  }
}